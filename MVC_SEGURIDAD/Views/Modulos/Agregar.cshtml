@model MVC_SEGURIDAD.Models.CRUD.MODULOScrudInsert
@{
    ViewBag.Title = "Agregar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Agregar</h2>
<div class="row">
    <div class="col-md-12">
        @using (Html.BeginForm("Agregar", "Modulos", FormMethod.Post, new { role = "form", @class = "" }))
        {
            @Html.AntiForgeryToken()

            @Html.ValidationMessage("error", new { @class = "tex-danger" })

            @Html.LabelFor(d => d.NOMBRE)
            @Html.TextBoxFor(d => d.NOMBRE, new { @class = "form-control" })
            @Html.ValidationMessage("USUARIO", new { @class = "text-danger" })

            <div style="text-align:right">
                <input type="submit" class="btn btn-success" value="Guardar" />

            </div>

        }
    </div>
</div>
